*** Variables ***
# Locator dung chung cho nhieu page/luong (header, dashboard, popup...)
# Them locator moi vao day khi thay no xuat hien o >=2 page khac nhau

${LBL_DASHBOARD}            css=img[alt="logo"]
